import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactCompiler: true,
  // cacheComponents: true, // Temporarily disabled due to 3rd party incompatibility
};

export default nextConfig;
